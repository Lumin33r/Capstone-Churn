apiVersion: v1
kind: Service
metadata:
  name: churn-wrapper-service
  namespace: retention-engine
  labels:
    app: churn-wrapper
spec:
  selector:
    app: churn-wrapper
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP