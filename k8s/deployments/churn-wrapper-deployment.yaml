apiVersion: apps/v1
kind: Deployment
metadata:
  name: churn-wrapper-deployment
  namespace: retention-engine
  labels:
    app: churn-wrapper-deployment
    team: retention-engine
spec:
  replicas: 2
  selector:
    matchLabels:
      app: churn-wrapper-deployment
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: churn-wrapper-deployment
    spec:
      containers:
        - name: churn-wrapper-deployment
          image: ghcr.io/lumin33r/churn-wrapper-deployment:latest
          ports:
            - containerPort: 8000             
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
